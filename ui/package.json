{
    "name": "ui",
    "version": "1.0.0",
    "description": "",
    "main": "dashboard.js",
    "scripts": {
        "start": "(npx snowpack dev &) && swa start http://localhost:8080 --api ../api",
        "test": "echo \"No tests yet...\"",
        "build": "npx snowpack build && mkdir _build && cp build/*.js build/*.html build/*.map _build && rm -rf build && mv _build build"
    },
    "dependencies": {
        "lit": "^2.0.0-rc.4",
        "powerbi-client": "^2.18.5",
        "redux": "^4.1.1"
    },
    "devDependencies": {
        "rollup": "^2.56.3",
        "rollup-plugin-minify-html-literals": "^1.2.6",
        "rollup-plugin-terser": "^7.0.2",
        "snowpack": "^3.8.8",
        "snowpack-plugin-rollup-bundle": "^0.4.4"
    },
    "keywords": [],
    "author": "",
    "license": "ISC"
}
