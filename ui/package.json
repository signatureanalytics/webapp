{
    "name": "ui",
    "version": "1.0.0",
    "description": "",
    "main": "dashboard.js",
    "scripts": {
        "start": "scripts/start.sh",
        "test": "if [[ ! -d 'build' ]]; then npm run build; fi; scripts/spawnWaitSpawn.js 'swa start build --api ../api' 'emulator started' 'npx playwright test'",
        "build": "npx snowpack build && mkdir _build && cp build/*.js build/*.html build/*.map _build && rm -rf build && mv _build build"
    },
    "dependencies": {
        "@reduxjs/toolkit": "^1.6.1",
        "lit": "^2.0.0-rc.4",
        "powerbi-client": "^2.18.5",
        "pwa-helpers": "^0.9.1"
    },
    "devDependencies": {
        "@azure/static-web-apps-cli": "^0.7.1",
        "@playwright/test": "^1.14.1",
        "azure-functions-core-tools": "^3.0.3734",
        "rollup": "^2.56.3",
        "rollup-plugin-minify-html-literals": "^1.2.6",
        "rollup-plugin-terser": "^7.0.2",
        "snowpack": "^3.8.8",
        "snowpack-plugin-rollup-bundle": "^0.4.4",
        "stream-snitch": "0.0.3"
    },
    "keywords": [],
    "author": "",
    "license": "ISC"
}
